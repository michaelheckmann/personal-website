{
  "name": "astro-app-with-cf-bindings",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler pages dev",
    "deploy": "astro build && wrangler pages deploy",
    "cf-typegen": "wrangler types",
    "astro": "astro",
    "wrangler": "wrangler"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.2.1",
    "astro": "^5.2.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "wrangler": "^3.107.3"
  }
}
